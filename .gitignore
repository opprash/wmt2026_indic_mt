# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# ---- Secrets ----
# never commit API keys; use the DASHSCOPE_API_KEY env var instead
*api_key*
*secret*

# ---- Large data / intermediate artifacts (regenerated by the pipeline) ----
# vocab building intermediates & datasets
sentence_vocab/
word_vocab/
dataset/
dataset_all/
gold_ecal_test/
gold_eval_test/
sft_testset/
sft_add_testset/
result/
words_dictionary.json
*.xlsx
*.zip

# ---- Model checkpoints / training outputs ----
*.safetensors
*.bin
*.pt
checkpoints/
train_save/
*_logs/

# ---- Misc data ----
testset/

# ---- KEEP committed for reproducibility ----
# The three final model-prediction folders are intentionally NOT ignored, so the
# official submission can be regenerated from them with make_all_submissions.py:
#   sft_predictions/  sft_add_predictions/  sft_test_predictions/
# (no global *_predicted.json ignore is used, so these track without negation.)

# ---- OS / editor / tooling (auto-created; never committed) ----
.DS_Store
Thumbs.db
.idea/
.vscode/
.codefree/
.venv/
